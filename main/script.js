// I want to make a simple javescipt game wiht rock paper scissors game. first step for javascript is to make the rules for the game. step 2 is to try to use img tags to for buttons.  And try to have fun with this\[T]/ -_-.


// let playGame = confirm("shall we play rock, paper or scissors")

// if (playGame) {
//   //play
//   let playerChoice = promt ('Please enter rock, paper, or scissors');
//   if (playerChoice) {

//   }
// } else {
//   alert('ok, maybe later');
// }

// good first part of javascript for me.
function game (x, y, z) {
    let x = rock;
    let y = paper;
    let z = scissors;
  if (x > z) {
      alert = "that hurt"
  }
   else (x < y); {
     alert = "wrong"
  }
   if (z < y) {
    alert = "that hurt"
  }
   else (z > x); {
    alert = "wrong"
  }
   if (y > x) {
    alert = "that hurt"
  }
   else (y < z); {
    alert = "that hurt"
  }
};
